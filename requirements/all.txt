# install everything optional (slow; prefer granular files)
-r rag.txt
-r prompting.txt
-r langchain-extra.txt
-r search.txt
-r fine-tuning.txt
# niche excluded by default — uncomment if needed:
# -r niche.txt
