# structured preference pairs
# authored jsonl rows: {id, instruction, constraint}
prompts_path: data/raw/structured_prompts.jsonl
processed_path: data/processed/structured
generations_path: data/processed/structured_generations.jsonl

# set via --generator-model (sft ckpt or teacher)
generator_model: null
max_tokens: 1024
temperature: 0.0
top_p: 1.0
batch_size: 8
