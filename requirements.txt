# post-training-experiments — default lean install
# pip install -r requirements.txt
# optional groups: pip install -r requirements/<group>.txt
#   crewai | autogen | rag | prompting | langchain-extra | search | fine-tuning | niche | all
# python >= 3.10 recommended

-r requirements/base.txt
